services:
  planner:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - planner-data:/data
    environment:
      - DATABASE_PATH=/data/planner.db
      - CANTON=ZH
    restart: unless-stopped

volumes:
  planner-data:
